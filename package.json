{
  "name": "ai-news-video-generator",
  "version": "0.1.0",
  "description": "Automated AI news video generation pipeline system",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "setup": "node -e \"require('child_process').execSync(process.platform === 'win32' ? 'setup.bat' : 'bash setup.sh', {stdio: 'inherit'})\"",
    "run:research": "ts-node src/scripts/run-research.ts",
    "run:script-gen": "ts-node src/scripts/run-script-generation.ts",
    "run:subtitle": "ts-node src/scripts/run-subtitle-generation.ts",
    "run:voice": "ts-node src/scripts/run-voice-synthesis.ts",
    "run:video": "ts-node src/scripts/run-video-composition.ts",
    "run:upload": "ts-node src/scripts/run-youtube-upload.ts",
    "run:pipeline": "ts-node src/scripts/run-pipeline.ts",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:integration": "jest --testPathPattern=tests/integration",
    "test:e2e": "jest --testPathPattern=tests/e2e"
  },
  "keywords": [
    "ai",
    "video-generation",
    "youtube",
    "automation",
    "pipeline"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "googleapis": "^130.0.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
